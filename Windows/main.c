#include "acllib.h"
#include <stdio.h>
/*
int Setup()
{
	initWindow("test",100,100,200,200);
	//initConsole();产生控制台 
	beginPaint();
	line(0,0,20,20);
	line(20,20,30,45);
	endPaint();
	putPixel(50,50,GREEN);
	return 0;
} 
*/
